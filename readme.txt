Reflect about your solution!

Summary:

Alle Befehle und die Verbindungen sollten funktionieren. Nur beim Server der !users Befehl gibt nur die Online User aus. Hatte leider keine Zeit das noch ganz zu 
implementieren.

Ich hab die Befehle beim Client mit einer BlockingQueue gelöst, es wartet also das Programm bis der Server geantwortet hat.
